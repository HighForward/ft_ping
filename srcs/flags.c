#include "../includes/ft_ping.h"

void perform_h()
{
    printf("\nUsage\n\tft_ping [options] <destination>\n");
    exit(1);
}

int toggle_flags(int flag)
{
//    if ((flag & (1 << H_BIT)) > 0)
//        perform_h();
//    if ((flag & (1 << V_BIT)) > 0)
//        printf("V ok\n");
    return (0);
}